#  SLAM 算法对比报告

> 本工程在**完全相同的**环境：同一标准世界、同一机器人、同一激光传感器、同一遥控轨迹下分别部署两种开源 SLAM 算法：
> - **方法一（基础）**：Cartographer（Google 开源，图优化 + CSM 前端）
> - **方法二（拓展）**：SLAM Toolbox（KartoGraph 衍生，在线异步）
>
> 本文档给出**对比方法、数据来源**与**实测结论**。

---

## 1. 对比方法

保证变量唯一：机器人型号、世界、激光参数、建图轨迹均不变，仅切换 SLAM 节点。

### 数据来源
1. **地图质量**：`analyze_map.py` 与 `compare_maps.py` 自动统计
   - 占用、空白、未知占比
   - 障碍连通分量数
   - 与理想世界的 **IoU 、 Precision 、 Recall of occupied**
   - 两图间一致性 IoU
2. **主观质量**：rviz2 截图对比——圆柱轮廓、墙厚、直线度、回环处重影

---

## 2. 对比数据表

### 2.1 地图质量

| 指标 | 理想值 | Cartographer（方法一） | SLAM Toolbox（方法二） |
|---|---|---|---|
| 占用率（occupied 占比） | 由世界算得 | 5.18% | 6.80% |
| 未知率 | 0% | 40.13% | **25.24%** |
| 障碍连通分量数 | 10（9 圆柱 + 1 墙） | 11 | 12 |
| 圆柱检出数与定位平均误差 | 9 根 、 0m | 9 根 、**0.023m** | 9 根 、 0.040m |
| 圆柱定位 RMSE | 0m | **0.027m** | 0.043m |
| IoU vs 理想世界 | 1.0 | **0.116** | 0.115 |
| Precision vs 理想世界 | 1.0 | **0.642** | 0.611 |
| Recall vs 理想世界 | 1.0 | 0.124 | 0.125 |
| 两 SLAM 图间 IoU | 1.0 | 0.305 | |

---

## 3. 结论

### 3.1 定位精度
两方法都准确定位了全部 9 根圆柱且轮廓无重影。以圆柱精确坐标衡量，**Cartographer 障碍定位
更准（平均 2.3cm vs 4.0cm，RMSE 2.7cm vs 4.3cm）**，与其更强的图优化、回环前端一致；
两图配准到同一理想世界后 IoU、Precision 几乎持平，
说明在 5m 级简单场景下二者对障碍的还原能力相当。


### 3.2 易用性与调参成本
- Cartographer：需维护 `robot_lds_2d.lua` + 两个被 include 的官方 lua，参数面广、调参门槛高。
- SLAM Toolbox：单个 YAML 即配置全部，官方默认参数在小场景大多可直用，仅需对齐激光量程。

### 3.3 适用场景结论
> 在本标准世界中：**SLAM Toolbox 以更低的调参成本达到与 Cartographer 相当的建图质量，
> 且覆盖更完整（未知区 25% vs 40%）**，更适合中小场景与快速部署；**Cartographer 障碍定位
> 精度更高（2.3cm vs 4.0cm）**，在需要更高定位精度、更复杂、更大场景或传感器噪声明显时
> 通常更具优势。两者在简单场景中的差异主要体现为覆盖完整度与定位精度±2cm 级，
> 均可满足后续 Navigation2 导航需求。

---

## 4. 附录：指标定义

- **IoU（交并比）** = `|A∩B| / |A∪B|`
- **Precision（精确率）** = `|A∩B| / |B|`（B 中被判占用的像素有多少真是障碍 → 误报越低越高）
- **Recall（召回率）** = `|A∩B| / |A|`（障碍被检出多少 → 漏检越低越高）
- **未知占比** = 像素值 205（灰）占比；越小越好（未知区意味着未探测/未回环）。
